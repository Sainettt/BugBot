# design/

`relay/` is a verbatim copy of the **Relay** design system made in Claude Design (owner: Danyil), version `1790069834-cd20`, copied 2026-09-22 from <https://claude.ai/artifact/So3gv1WgCpE5AZFHqaLy6S>. Layout is the Design System artifact's `project/` folder: `tokens.json` (colour / type / spacing / radius / shadow / size, dark + light), `components/bundle.css` (the `rl-*` component layer), `README.md` (brand book, Russian), `flows.md` (the four screens), `components/<Name>/README.md` + `preview.html` (guidelines and live previews), `components/Page*/preview.html` (page mockups), `design-system.json` (the artifact's index).

Relay's names and copy belong to another product; BugBot takes the look, not the words. What is used, renamed or deliberately changed is in [`docs/05-design-ui.md`](../docs/05-design-ui.md), which also describes the re-sync procedure. Do not edit files under `relay/` by hand — change them in Claude Design and copy again.
